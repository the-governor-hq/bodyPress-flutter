import 'package:flutter/material.dart';

import '../../body_blog/screens/body_blog_screen.dart';

/// Journal tab — wraps the existing BodyBlog experience.
class JournalScreen extends StatelessWidget {
  const JournalScreen({super.key});

  @override
  Widget build(BuildContext context) => const BodyBlogScreen();
}
